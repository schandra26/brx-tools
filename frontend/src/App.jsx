import React from 'react'

export default function App(){
  return (
    <div style={{padding:24,fontFamily:'Inter, Arial'}}>
      <h1>BRx Tools — Frontend (Dev)</h1>
      <p>This is a minimal Vite + React scaffold. Connect to the backend later at <code>/mappings</code> and <code>/admin</code>.</p>
    </div>
  )
}
