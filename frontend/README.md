# BRx Tools Frontend (Dev)

This is a minimal Vite + React scaffold to start the frontend developer workflow.

Quick start (PowerShell):

```powershell
cd frontend
npm install
npm run dev
```

The dev server runs on `http://localhost:5173` by default. Configure a proxy or call backend APIs directly.
